<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gen.auth.server.mapper.role.RoleMapper">
	<select id="getRoleByUid" resultType="com.gen.auth.server.entity.role.Role">
        SELECT r.* FROM role r JOIN user_role u ON r.id = u.role_id 
		WHERE u.user_id = #{userId} AND r.status = 1 AND r.del_flag = 0 AND u.del_flag = 0
    </select>
    <select id="getRoleMenu" resultType="java.lang.String">
    	SELECT u.uri_id FROM (
			SELECT r.sign, m.menu_id FROM role r JOIN role_menu m ON r.id = m.role_id 
		) m JOIN menu_uri u ON m.menu_id = u.menu_id WHERE m.sign = #{sign} GROUP BY u.uri_id
    </select>
</mapper>
