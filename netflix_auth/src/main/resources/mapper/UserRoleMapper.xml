<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gen.auth.server.mapper.user.UserRoleMapper">

    <select id="getSignByUid" resultType="java.lang.String">
    	SELECT sign FROM (
			SELECT role_id FROM user_role WHERE user_id = #{userId} AND del_flag = 0
		) m JOIN role e ON m.role_id = e.id
    </select>
    
    <select id="getRoleByUid" resultType="com.gen.auth.server.entity.user.UserRole">
        SELECT id, role_id, user_id, del_flag FROM user_role WHERE user_id = #{userId} AND del_flag = 0
    </select>

    <select id="getRoleByRid" resultType="com.gen.auth.server.entity.user.UserRole">
        SELECT * FROM user_role WHERE user_id = #{userId} AND role_id = #{roleId} LIMIT 1
    </select>

    <update id="update" parameterType="com.gen.auth.server.entity.user.UserRole">
    	UPDATE user_role SET del_flag = #{delFlag} WHERE id = #{id}
    </update>

    <update id="updateFlag" parameterType="com.gen.auth.server.entity.user.UserRole">
    	UPDATE user_role SET del_flag = #{delFlag} WHERE user_id = #{userId} AND role_id = #{roleId}
    </update>
</mapper>
